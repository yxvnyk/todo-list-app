@model TaskViewModel
@{
    this.Layout = "_TaskLayout";
}
<div class="container p-3">
    <h1> <span class="badge text-bg-secondary">Task details</span></h1>
    <div>
        <a class="btn btn-info btn-sm" asp-controller="Task" asp-action="Details" asp-route-productId="@Model?.Task?.Id">
            Details
        </a>
        <a class="btn btn-warning btn-sm" asp-controller="Task" asp-action="Edit" asp-route-id="@Model?.Task?.Id">
            Edit
        </a>
        <a class="btn btn-danger btn-sm" asp-controller="Admin" asp-action="Delete" asp-route-productId="@Model?.Task?.Id">
            Delete
        </a>
    <table class="table table-sm table-bordered table-striped">
        <tbody>
            <tr>
                <th>Title</th>
                <th>@Model?.Task?.Title</th>
            </tr>
            <tr>
                <th>Status</th>
                <th>@Model?.Task?.Status</th>
            </tr>
            <tr>
                <th>Description</th>
                <th>@(Model?.Task?.Description ?? "-")</th>
            </tr>
            <tr>
                <th>Date created</th>
                <th>@Model?.Task?.DateCreated</th>
            </tr>
            <tr>
                <th>Due date</th>
                <th>@Model?.Task?.DueDate</th>
            </tr>
            <tr>
                <th>Assignee</th>
                <th>@Model?.Task?.AssigneeId</th>
            </tr>
            <tr>
                <th>Tags</th>
                <th>@{
                        if (Model?.Tags != null)
                        {
                            foreach (var tag in Model.Tags)
                            {
                              <text>#@tag.Name,</text>  
                            };
                        }
                    }</th>
            </tr>
        </tbody>
    </table>
    </div>
    <div class="my-3 p-3 bg-body rounded shadow-sm">
        <h6 class="border-bottom pb-2 mb-0">Comments</h6>
        @{
            if (Model?.Comments != null)
            {
                foreach (var com in Model.Comments)
                {
                    <div class="d-flex text-body-secondary pt-3">
                        <svg class="bd-placeholder-img flex-shrink-0 me-2 rounded" width="32" height="32" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: 32x32" preserveAspectRatio="xMidYMid slice"><title>Placeholder</title><rect width="100%" height="100%" fill="#007bff" /><text x="50%" y="50%" fill="#007bff" dy=".3em">32x32</text></svg>
                        <p class="pb-3 mb-0 small lh-sm border-bottom">
                            <strong class="d-block text-break text-gray-dark">@com.AuthorId</strong>
                            <span class="d-block text-break">@com.Comment</span>
                        </p>
                    </div>
                };
            }
        }
        
        @* <div class="d-flex text-body-secondary pt-3"> *@
        @*     <svg class="bd-placeholder-img flex-shrink-0 me-2 rounded" width="32" height="32" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: 32x32" preserveAspectRatio="xMidYMid slice"><title>Placeholder</title><rect width="100%" height="100%" fill="#e83e8c" /><text x="50%" y="50%" fill="#e83e8c" dy=".3em">32x32</text></svg> *@
        @*     <p class="pb-3 mb-0 small lh-sm border-bottom"> *@
        @*         <strong class="d-block text-gray-dark">@com.Comment</strong> *@
        @*         Some more representative placeholder content, related to this other user. Another status update, perhaps. *@
        @*     </p> *@
        @* </div> *@
        @* <div class="d-flex text-body-secondary pt-3"> *@
        @*     <svg class="bd-placeholder-img flex-shrink-0 me-2 rounded" width="32" height="32" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: 32x32" preserveAspectRatio="xMidYMid slice"><title>Placeholder</title><rect width="100%" height="100%" fill="#6f42c1" /><text x="50%" y="50%" fill="#6f42c1" dy=".3em">32x32</text></svg> *@
        @*     <p class="pb-3 mb-0 small lh-sm border-bottom"> *@
        @*         <strong class="d-block text-gray-dark">@com.Comment</strong> *@
        @*         This user also gets some representative placeholder content. Maybe they did something interesting, and you really want to highlight this in the recent updates. *@
        @*     </p> *@
        @* </div> *@
        <small class="d-block text-end mt-3">
            <a href="#">All comments</a>
        </small>
    </div>
    <a class="btn btn-secondary"
       href="@Model?.ReturnUrl">Back</a>
</div>