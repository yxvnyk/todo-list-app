@model (TaskDto Task, Uri ReturnUrl)
@{
    Layout = "_NavLayout";
}
<div class="container-sm">
    <partial name="_TaskEditor.cshtml" model='new TaskEditorViewModel()
{
    Task = @Model.Task,
    CallbackMethodName = "Create",
    ThemeColor = "warning",
    Title = "Create",
    ReturnUrl = @Model.ReturnUrl,
}' />
</div>
@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
  }